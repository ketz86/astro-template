import type { App } from 'vue';

// const { RECAPTCHA_KEY } = import.meta.env

export default (app: App) => {
  // app.use(VueReCaptcha, { siteKey: RECAPTCHA_KEY, loaderOptions: { autoHideBadge: true } });
  // app.use(InstantSearch);
  
  // app.component("Popper", Popper);
};